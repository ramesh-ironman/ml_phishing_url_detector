# ml_phishing_url_detector
checking given url is safe or not

it use random forest classifier model

# requirements
`pip install scikit-learn pandas numpy flask requests beautifulsoup4 joblib matplotlib bandit`

run app.py ..... listen port on 5000

